"""Shared internals of the tabular-foundation-model baselines.

Used by `rdblearn` and the `tabpfn-rel` variants.

`tfm` carries the TFM registry: the single seam for adding a new foundation model.
"""
